<!DOCTYPE html>
<html>
	<head>
		<title><%=title%></title>
		<style type="text/css">
			.wph
			{
				display: none;
			}
			.loadError
			{
				padding: 5px;
				margin: 5px;
				background: #c00;
				color: #fff;
			}
			<%=style%>
		</style>
		<script type="text/javascript" src="<%=prefix%>/boot"></script>
		<%=extScript%>
		<% if(script && script.match(/\S/)) { %>
			<script type="text/javascript">
				<%=script%>
			</script>
		<% } %>
	</head>
	<% if(widget.rootTagName != "body") { %><body><% } %>
		<%=body%>
	<% if(widget.rootTagName != "body") { %></body><% } %>
</html>
